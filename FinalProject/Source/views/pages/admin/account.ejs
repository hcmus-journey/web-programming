<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../../layouts/head', { title: 'HATShop - Account' }) %>
</head>

<body>
  <%- include('../../layouts/adminHeader') %>

    <%- include('../../layouts/adminNavbar', {isLoggedIn: isLoggedIn}) %>

    <%- include('../../layouts/notification') %>

      <!-- breadcrumb -->
      <div class="container py-4 flex items-center gap-3">
        <a href="/" class="text-primary text-base">
          <i class="fa-solid fa-house"></i>
        </a>
        <span class="text-sm text-gray-400">
          <i class="fa-solid fa-chevron-right"></i>
        </span>
        <p class="text-gray-600 font-medium">Account</p>
      </div>
      <!-- ./breadcrumb -->

      <!-- account wrapper -->
      <div class="container grid grid-cols-11 items-start gap-4 pt-4 pb-16">
        <!-- sidebar -->
        <div class="col-span-5 lg:col-span-3">
          <div class="px-4 py-3 shadow flex items-center gap-4">
            <div class="flex-shrink-0">
              <img src="<%= user.image_url ? user.image_url : '/public/assets/images/user/default-avatar.jpg' %>" alt="profile"
                class="rounded-full w-14 h-14 border border-gray-200 p-1 object-cover avatar" />
            </div>
            <div class="flex-grow">
              <p class="text-gray-600">Hello,</p>
              <h4 class="text-gray-800 font-medium name"> 
                <%= user.name %>
            </h4>
            </div>
          </div>

          <div class="mt-4 bg-white shadow rounded p-4 divide-y divide-gray-200 space-y-4 text-gray-600">
            <div class="space-y-1 lg:pl-8">
              <a href="/admin/account" class="relative text-primary block font-medium capitalize transition">
                <span class="hidden lg:block absolute -left-8 top-0 text-base">
                  <i class="fa-regular fa-address-card"></i>
                </span>
                Profile information
              </a>
              <a href="/change-password" class="relative hover:text-primary block capitalize transition">
                Change password
              </a>
            </div>

            <div class="space-y-2 lg:pl-8 pt-4">
              <a href="/logout" class="relative hover:text-primary block font-medium capitalize transition">
                <span class="hidden lg:block absolute -left-8 top-0 text-base">
                  <i class="fa-solid fa-right-from-bracket"></i>
                </span>
                Logout
              </a>
            </div>
          </div>
        </div>
        <!-- ./sidebar -->

        <!-- info -->
        <div class="shadow rounded bg-white px-4 pt-6 pb-8 col-span-6 lg:col-span-8" id="profile" data-role="admin">
          <div class="flex items-center justify-between mb-4">
            <h3 class="font-medium text-gray-800 text-lg">Admin Profile</h3>
            <button class="text-primary" id="edit-btn">Edit</button>
          </div>

          <div class="space-y-1">
            <p class="text-gray-800"><span class="font-bold">Name: </span><span class="name"><%= user.name%></span></p>
            <p class="text-gray-800"><span class="font-bold">Email: </span> <span><%= user.email%></span></p>
          </div>
        </div>

        <div class="shadow rounded bg-white px-4 pt-6 pb-8 col-span-6 lg:col-span-8 hidden" id="edit-profile">
          <%- include('../../layouts/accountEdit') %>
        </div>
        <!-- ./info -->
      </div>
      <!-- ./account wrapper -->

      <%- include('../../layouts/footer') %>
      <script type="module" src="/public/js/Account.js"></script>
</body>

</html>